meta {
  name: Get Gift (Rekening)
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/undangan/:id/gifts/:giftId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # Get Gift (Rekening)

  Ambil detail rekening berdasarkan ID.
}
