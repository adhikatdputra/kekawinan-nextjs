meta {
  name: Delete Gift (Rekening)
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/api/undangan/:id/gifts/:giftId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # Delete Gift (Rekening)

  Hapus rekening bank dari undangan.
}
