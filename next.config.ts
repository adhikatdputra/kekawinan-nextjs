import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["localhost", "kekawinan.com", "res.cloudinary.com", "cdn.trakteer.id"],
  },
};

export default nextConfig;
